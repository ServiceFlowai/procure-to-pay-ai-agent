import { useState } from 'react';type Message = { id: number; senderName: string; timestamp: string; content: string;};export const useMessages = () => { const [messages, setMessages] = useState<Message[]>([{ id: 1, senderName: 'Supplier A', timestamp: '2023-10-01 10:00', content: 'Hello, we have shipped your order.' }]); const sendMessage = (supplierId: string, content: string) => { setMessages([...messages, { id: messages.length + 1, senderName: `Supplier ${supplierId}`, timestamp: new Date().toISOString(), content }]); }; return { messages, sendMessage };};