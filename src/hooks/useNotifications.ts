import { useState } from 'react';type Notification = { id: number; message: string;};export const useNotifications = () => { const [notifications, setNotifications] = useState<Notification[]>([{ id: 1, message: 'Order ORD001 is overdue.' }]); const markAsRead = (id: number) => { setNotifications(notifications.filter(notification => notification.id !== id)); }; return { notifications, markAsRead };};