import { useState } from 'react';type Order = { id: number; orderNumber: string; supplierName: string; orderDate: string; status: string;};export const useOrders = () => { const [orders, setOrders] = useState<Order[]>([{ id: 1, orderNumber: 'ORD001', supplierName: 'Supplier A', orderDate: '2023-10-01', status: 'Pending' }]); const createOrder = (order: Omit<Order, 'id'>) => { setOrders([...orders, { ...order, id: orders.length + 1 }]); }; const updateOrder = (id: number) => { setOrders(orders.map(order => order.id === id ? { ...order, status: 'Completed' } : order)); }; const deleteOrder = (id: number) => { setOrders(orders.filter(order => order.id !== id)); }; return { orders, createOrder, updateOrder, deleteOrder };};