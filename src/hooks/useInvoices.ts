import { useState } from 'react';type Invoice = { id: number; invoiceNumber: string; supplierName: string;};export const useInvoices = () => { const [invoices, setInvoices] = useState<Invoice[]>([{ id: 1, invoiceNumber: 'INV001', supplierName: 'Supplier A' }]); const uploadInvoice = (file: File) => { console.log('Uploading:', file.name); setInvoices([...invoices, { id: invoices.length + 1, invoiceNumber: `INV00${invoices.length + 1}`, supplierName: 'Supplier B' }]); }; return { invoices, uploadInvoice };};