import React from 'react';import { useAuditLog } from '../hooks/useAuditLog';const AuditLog: React.FC = () => { const { logs } = useAuditLog(); return (<div className="p-6"><h1 className="text-3xl mb-4">Audit Log</h1><table className="w-full border"><thead><tr><th>User ID</th><th>Action</th><th>Timestamp</th><th>Entity</th></tr></thead><tbody>{logs.map(log => (<tr key={log.id}><td>{log.userId}</td><td>{log.action}</td><td>{log.timestamp}</td><td>{log.entity}</td></tr>))}</tbody></table></div>);};export default AuditLog;