import React, { useState } from 'react';import { useReports } from '../hooks/useReports';const Reports: React.FC = () => { const { generateReport } = useReports(); const [criteria, setCriteria] = useState({ dateRange: '', status: '', supplier: '' }); const handleGenerate = () => { generateReport(criteria); }; return (<div className="p-6"><h1 className="text-3xl mb-4">Reports</h1><div className="mb-4"><label className="block text-gray-700">Date Range</label><input type="text" value={criteria.dateRange} onChange={(e) => setCriteria({ ...criteria, dateRange: e.target.value })} className="w-full p-2 border rounded" /></div><div className="mb-4"><label className="block text-gray-700">Status</label><input type="text" value={criteria.status} onChange={(e) => setCriteria({ ...criteria, status: e.target.value })} className="w-full p-2 border rounded" /></div><div className="mb-4"><label className="block text-gray-700">Supplier</label><input type="text" value={criteria.supplier} onChange={(e) => setCriteria({ ...criteria, supplier: e.target.value })} className="w-full p-2 border rounded" /></div><button onClick={handleGenerate} className="bg-blue-500 text-white p-2 rounded">Generate Report</button></div>);};export default Reports;